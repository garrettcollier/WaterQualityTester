import 'package:flutter/foundation.dart';
import 'package:flutter/material.dart';
import 'package:water_quality_app/rgb_generator.dart';

// get image bytes and extract colors and sort
// Future<void> extractAverageSampleColor() async {
//   var imageSampleBytes = await widget.image.readAsBytes();
//   var sampleColors = await compute(extractPixelsColors, imageSampleBytes);
// }
